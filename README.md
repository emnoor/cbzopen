# cbzopen - open a cbz file in your web browser

cbzopen extracts your cbz file into a temporary directory,
creates an index.html file with the images
and starts a HTTP server to be viewed in a web browser.
